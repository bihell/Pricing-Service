{% extends "base.jinja2" %}
{% block content %}
    <div class="page-header mb-3">
        <h2>Alert
            <small>showing already existing alerts</small>
        </h2>
        {#            <small>for '{{ alert.item.name }}'</small>#}
    </div>
    <div class="list-group mb-3">
        {% for alert in alerts %}
            <a href="{{ url_for('alerts.get_alert_page',alert_id=alert._id) }}" class="list-group-item list-group-item-action">
                <h5>Alert for {{ alert.item.name }}</h5>
                <p>Last price found <strong>{{ alert.item.price }}</strong><br/>
                    Last checked on <strong>{{ alert.last_checked.strftime("%Y-%m-%d at %H:%M") }}</strong></p>
            </a>
        {% endfor %}
    </div>
    <a class="btn btn-success" href="{{ url_for('alerts.create_alert') }}"><span class="oi oi-plus"></span> Create new</a>
    {#    <p>Last price found <strong>£{{ alert.item.price }}</strong></p>#}
    {#    <p>Last checked on <strong>{{ alert.last_checked.strftime("%d-%m-%Y %H:%M") }}</strong></p>#}
    {#    <div class="panel panel-default">#}
    {#        <div class="panel-body">#}
    {#            <a class="btn btn-default" href="{{ alert.item.url }}" role="button">Visit Site</a>#}
    {#            <a class="btn btn-default" href="{{ url_for('alerts.edit_alert', alert_id=alert._id) }}" role="button">Edit Alert</a>#}
    {#            {% if alert.active %}#}
    {#                <a class="btn btn-danger" href="{{ url_for('alerts.deactivate_alert', alert_id=alert._id) }}" role="button">Deactivate Alert</a>#}
    {#            {% else %}#}
    {#                <a class="btn btn-success" href="{{ url_for('alerts.activate_alert', alert_id=alert._id) }}" role="button">Activate Alert</a>#}
    {#                <a class="btn btn-danger" href="{{ url_for('alerts.delete_alert', alert_id=alert._id) }}" role="button">Permanently Delete Alert</a>#}
    {#            {% endif %}#}
    {#            <a class="btn btn-warning" href="{{ url_for('alerts.check_alert_price', alert_id=alert._id) }}" role="button">Check Price Now</a>#}
    {#        </div>#}
    {#    </div>#}
{% endblock %}