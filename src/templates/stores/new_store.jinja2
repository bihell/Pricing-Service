{% extends "base.jinja2" %}
{% block content %}
    <div class="page-header mb-3">
        <h1>Create Store</h1>
    </div>
    <div class="form-size">
        <form method="post">
            <div class="form-group">
                <label for="name">Store name</label>
                <input type="text" class="form-control" id="name" name="name" placeholder="Name">
            </div>
            <div class="form-group">
                <label for="url_prefix">Store URL Prefix</label>
                <input type="text" class="form-control" id="url_prefix" name="url_prefix" placeholder="http://..."
                aria-describedby="storeURLPrefixHelp">
                <span id="storeURLPrefixHelp" class="text-muted">What characters do URLs of all items in this online store start with?</span>
            </div>
            <div class="form-group">
                <label for="tag_name">Tag name</label>
                <input type="text" class="form-control" id="tag_name" name="tag_name" placeholder="span" value="span"
                       aria-describedby="tagNameHelp">
                <span id="tagNameHelp" class="text-muted">What is the HTML tag of the item that contains the item price? (usually 'span')</span>
            </div>
            <div class="form-group">
                <label for="query">Query</label>
                <input type="text" class="form-control" id="query" name="query" placeholder='{"id": "..."}'
                       aria-describedby="queryHelpBlock">
                <span id="queryHelpBlock" class="text-muted">What JSON query would we have to do to find the specific tag with the price?</span>
            </div>
            <button type="submit" class="btn btn-primary">Create</button>
        </form>
    </div>
{% endblock %}